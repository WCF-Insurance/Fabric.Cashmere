import { PickerItemDirective } from './picker-item.directive';

describe('PickerItemDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PickerItemDirective();
    expect(directive).toBeTruthy();
  });
});
