import { MultiSelectPickerSummaryPipe } from './multi-select-picker-summary.pipe';

describe('MultiSelectPickerSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new MultiSelectPickerSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
